#!/usr/bin/env bash
# pre-push: runs on every `git push`. Blocks the push on any failure.
# TRULAYER_DRY_RUN=true prevents live network calls during smoke tests.
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"

fail() { echo "❌ $*" >&2; exit 1; }
step() { echo "▶ $*"; }

step "demo-python: smoke tests"
TRULAYER_DRY_RUN=true uv run pytest tests/ || fail "tests failed"

echo "✅ pre-push checks passed"
